{
  "name": "mlh-frontend",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "postinstall": "./init.sh && soroban contract bindings typescript --wasm ../target/wasm32-unknown-unknown/release/mlh_contract.wasm --network standalone --contract-id $(cat ./contract.id) --contract-name Million --output-dir node_modules/Million"
  },
  "dependencies": {
    "@astrojs/node": "^5.3.4",
    "@stellar/freighter-api": "^1.6.0",
    "astro": "^2.10.1",
    "soroban-client": "^0.10.1"
  }
}
